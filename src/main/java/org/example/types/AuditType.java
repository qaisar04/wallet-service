package org.example.types;

/**
 * Перечисление `AuditType` определяет два возможных типа результата аудиторской записи.
 * Эти типы используются для указания результата выполненного действия в аудиторских записях.
 */
public enum AuditType {
    SUCCESS,
    FAIL
}
